---
# The playbook defines how to configure all
# Rugby services

- hosts: db
  roles:
    - { role: mongodb, when: db_type == 'MongoDB' } 

- hosts: env
  roles:
    - { role: nodejs, when: env_type == 'NodeJS' }

